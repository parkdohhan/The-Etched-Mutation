# W3 구현 보고 — 위계 어휘 철거 · 셸 정리 · 접촉 희소성 측정 (2026-07-16)

> 핸드아웃: docs/이본지층/핸드아웃_W3_어휘셸측정-260716.md
> 정본: docs/이본지층/이본지층_설계_v1-260716.md (결정 1·3)
> 미션: "트루엔딩/공명=보상" 위계를 문서·셸·문자열에서 걷어내고, 접촉 임계의 현재 발화율을 측정.
> **어휘를 바꾼 것이지 기능을 새로 만든 게 아니다.** 연출·배선은 W4.

## 커밋 요약

| 항목 | 커밋 | 내용 |
|---|---|---|
| W3-1 | `e1bd81a` | CLAUDE.md §4 용어 정정 + §6.3 원칙 주석 |
| W3-3 | `19b1a1a` | 셸 어휘 은퇴 (endScreen·index.html·npc) |
| W3-2 | `00bc8c2` | 문서 스윕 (규범 2 본문 + 역사 18 주석) |
| W3-4 | `33c4e41` | 접촉 희소성 측정 (스크립트 + 보고서) |
| W3-5 + 본 보고 | (이 커밋) | 구역 밖 잔재 지도 + 구현 보고 |

전 커밋 `node --check` + `npm test`(581 passed) 통과. 큰따옴표 없는 커밋 메시지. 배포·마이그레이션·DB 쓰기 없음(측정은 SELECT만).

---

## W3-1 CLAUDE.md §4 용어 정정 — 커밋 `e1bd81a`

grep 증빙: `CLAUDE.md:108-109` (공명·trajectory bridge 행), `:186` (§6.3 Design principle).

**공명 (resonance) 행**
- 전: `| **공명 (resonance)** | 트루엔딩 도달. 궤적 브릿지 생성 트리거 | 인기도/좋아요 아님 |`
- 후: `| **공명 (resonance) → 접촉 (contact)** | 접촉 — 여정 중 유령과 정말 겹치는 드문 사건 (회차당 최대 1회 표시). 승리 상태 아님. 인기도/좋아요 아님 | 260716 이본지층 재정의: 트루엔딩 폐기 |`
  - (옛 용어 "공명(resonance)"을 검색 가능하게 병기)

**trajectory bridge 행**
- 전: `| **trajectory bridge** | 공명 엔딩 도달자의 궤적이 자동 변환된 브릿지 (동적, 자동 승인) | |`
- 후: `| **trajectory bridge** | 접촉 기록 (2026-07-16 전용) — 접촉 순간의 발화·씬·시각. 정본화 없음 | |`

**§6.3 원칙 완성 주석** (핸드아웃 허용 — 트루엔딩 언급은 §6.3에 없었음, 원칙 아래 1줄 추가)
- "엔진은 판단하지 않는다" 원칙 아래에 재정의가 원칙을 완성한다는 주석 1줄. 공식·출력 불변, 어휘만.
- `echo_words`의 "공명 단어"(잔향 뜻)는 위계와 무관 → 유지.

## W3-3 셸 어휘 은퇴 — 커밋 `19b1a1a`

grep 증빙 전/후: `js/app/endScreen.js`, `js/npc-dialogues.js`, `index.html`.

- **endScreen.js:239** (사용자 문구, 핵심)
  - 전: `<strong>You reached the true ending.</strong><br><br>Your emotional structure nearly overlapped with theirs.<br>This alignment will be deeply etched into the original strata.`
  - 후: `<strong>For a moment, your trajectory and theirs overlapped.</strong><br><br>Not a victory — a rare contact.<br>This variant settles as a thin layer in the strata.`
  - ("승리" 언어 제거, 접촉·현재시제·이본. ★작가 감수 필요 — 아래 목록)
- **index.html:504** 배지 텍스트: `TRUE ENDING` → `CONTACT` (class·id 유지 — CSS는 구역 밖, id는 getElementById 소비자).
- **npc-dialogues.js:112** 키: `trueEnding:` → `endingSettled:` (값 불변 — 이미 중립 이본 문구). 유일 소비자 `endScreen.js:288` 동시 갱신.
- 로그 문자열: `[Ending] 트루엔딩 표시` → `접촉 엔딩 표시`, `트루엔딩 쪽지 UI 로드 error` → `접촉 엔딩 쪽지 UI 로드 error`.

**유지한 식별자 (의도적)**: `isTrueEnding` 변수(엔진 출력 `alignmentResult.isTrueEnding` 소비 — 분류 로직 금지), DOM id `trueEndingBadge`(CSS 클래스 `.true-ending-badge`는 css/ 구역 밖). 문자열만 바꿨고 기능·분기 로직은 불변.

## W3-2 문서 스윕 — 커밋 `00bc8c2`

grep 대상: `트루엔딩|true ending|공명 엔딩` 전수. `docs/별이엔진_V4-*.md` = **해당 어휘 0건**(수정 불요).

**규범 문서 본문 수정 (2건):**
- `TEM_시스템_매뉴얼-260410.md:278` `notes — 트루엔딩 도달 시...` → `notes — 접촉 엔딩(여정 중 유령과 겹치는 드문 순간) 시...`
- `TEM_시스템_매뉴얼-260410.md:295` `generate-reveal | 트루엔딩 리빌` → `엔딩 리빌 (여정 → 이본 서사 재구성)`
- `데모기억_풀기능_체크리스트-260707.md:102` `공명 엔딩 도달 1회 → trajectory_bridges 생성...` → `접촉 사건 1회 (…승리 아님) → trajectory_bridges(접촉 기록)…`

**역사 문서 보존 처리 (18건, 원문 보존 + 상단 [260716 이본지층] 주석 1줄):**
critic_input_LUMEN-260428 · TEM_코드_까봤다_전체지도-260518 · _merged/01·02·05·06·07 · 데이터계약_브릿지_v1-260412 · 기억유전학_v0.3 · paper/TrackA_…-260419 · 메모/Feedback · 용어_전수조사_원자료-260415 · 정렬도_파동_계산_방식-260324 · 생성형_지형모델_v2-260619 · 초심_정합성_점검-260516 · 유령시스템_확정_v1-260418 · 점검/R5_수리보고-260714 · 점검/L2_발견-260713.

맞춤 문구(다른 기술 용법 혼동 방지): 유령시스템(대화 분기 "공명도=turn-level alignment"는 유효, 손대지 않음) · 데이터계약(trajectory_bridges=접촉 기록, ALTER는 W2) · 지형모델v2(§8 매듭 해소) · 정렬도(정렬도 계산 자체는 유효).

## W3-4 접촉 희소성 측정 — 커밋 `33c4e41`

산출: 스크립트 `tools/contact_rate_probe.mjs` + 보고서 → [접촉희소성_측정-260716.md](접촉희소성_측정-260716.md).

**임계 권고 한 줄: 0.85 유지.** 실관객 29턴은 0.85에서도 접촉 0건(최대 코사인 0.692) — 실관객에겐 이미 드묾. ≥0.85 접촉은 전부 페르소나(시뮬) 플레이. 상향(0.90/0.92)은 실관객엔 무의미(이미 0)하고 시뮬 꼬리만 깎음. 진짜 레버 = anchor 배치 + 비교 대상(W4). **최종 결정은 지휘·작가.**

utterances(잔상 8행) = emotion_tags+axis_x/z 표현이라 12축 코사인 비교 불가 + 기억 미연결 → 측정 제외.

## W3-5 구역 밖 어휘 잔재 지도 (수정 없음 — W4 배선 참고)

grep `트루|true.?ending|공명 엔딩|isTrueEnding|trueEnding`:
- **js/ui: 0건. js/core: 0건. play-test.html: 0건.** → 엔진·상영 경로엔 트루엔딩 없음 (설계 §1 재확인).
- **supabase/migrations/20250107000000_create_notes_table.sql** :1·2·42·44 — SQL 코멘트 "true ending note system", "reaching true ending" (기능 아님, 코멘트). 구역 밖(W2).
- **js/index.js:39** — `showTrueEndingNoteUI` import 재수출 (구역 밖). 이 참조 때문에 함수명 리네임 보류.
- **css/index.css** — `.true-ending-badge` 클래스 정의 (구역 밖). index.html 배지 id·class는 그대로 두어 스타일 유지.
- **todo.md:170·179** (루트, 구역 밖) — 코드맵 설명의 "트루엔딩/일반 분기" 표현.

**구역 내지만 W3-3 대상 아님 (= 기능/분류 로직, W4 소관):**
- `js/app/comparison.js:18` `isTrueEnding: (…currentAlignment…) >= 0.65` — 접촉 판정 임계 로직. `:287-298` `comparisonTrueEndingBadge` 표시, `:485` 기본값. **분류 로직이라 손대지 않음**(핸드아웃 "하지 말 것").
- `js/app/auth.js:152-153·221` `// True ending note UI` 코멘트 + `showTrueEndingNoteUI` 함수 정의/수출 — js/index.js(구역 밖) 소비 → 리네임 보류.

---

## 못 한 것 / 보류 (의도적)

1. **함수명 `showTrueEndingNoteUI` 미변경** — js/index.js:39(구역 밖)가 참조. 리네임하면 구역 밖 파일 수정 필요 → 보류. W4가 index 셸 정리 시 함께.
2. **comparison.js `isTrueEnding` 분류 로직(≥0.65) 미변경** — 어휘가 아니라 기능. 핸드아웃 명시적 금지 항목. W4 소관.
3. **DOM id `trueEndingBadge` / CSS 클래스 `.true-ending-badge` 미변경** — id 소비자(endScreen)·CSS(css/ 구역 밖) 정합 유지 위해 가시 텍스트만 교체.
4. **probe 스크립트 로컬 미실행** — `@supabase/supabase-js` 루트 미설치(기존 tools/*.mjs 동일 전제). 동일 코사인 로직을 Supabase MCP SQL로 실행해 실측 확보. `node --check` 통과.
5. **접촉 비교 대상 미확정** — 본 측정은 "씨앗 전체 최근접"(상한). 실제 이름-드러남이 "뽑힌 드리프트 유령 1개"와 비교하는지 W4 확인 후 재측정 권고.

## 작가 감수 필요 (임시 중립 문구 — 작가 목소리로 다듬을 것)

1. **endScreen.js:239 접촉 엔딩 문구** — ✅ 2026-07-16 작가(사용자) 확정: 세 줄 중 첫 줄만 남기고 나머지 두 줄 삭제 + 한/영 이중언어. 현재: `<strong>잠깐, 당신의 궤적과 그의 궤적이 겹쳤다.</strong> / For a moment, your trajectory and theirs overlapped.`
2. **index.html 배지 `CONTACT`** — 짧은 라벨 임시 선택. "접촉" / "A RARE CONTACT" / 이중언어 등 작가 결정.
3. **매뉴얼·체크리스트 재서술** ("접촉 엔딩", "접촉 사건", "엔딩 리빌") — 규범 문서 표현, 작가 확인 권장.
