# 구현 핸드아웃 W3 — 위계 어휘 철거 · 셸 정리 · 접촉 희소성 측정

> 정본: docs/이본지층/이본지층_설계_v1-260716.md (결정 1·3, §1 사실 확인).
> 미션: "트루엔딩/공명=보상" 위계를 문서·셸·문자열에서 걷어내고, 접촉 임계의 현재 발화율을 측정해 보고한다.
> 원칙: **어휘를 바꾸는 것이지 기능을 새로 만드는 게 아니다.** 연출·배선은 W4 몫.

## 절대 규칙
1. **내 구역만**: `docs/**`(CLAUDE.md 포함 — W3 단독), `index.html`, `js/app/**`, `js/lib/i18n.js`, `js/npc-dialogues.js`, `tools/**`.
2. play-test·js/ui·js/core·js/shared·supabase 금지 (그쪽 어휘 잔재는 발견 시 보고서에 목록만).
3. 배포·마이그레이션·DB 쓰기 금지. DB SELECT + MCP 읽기 허용 (측정용).
4. 삭제·교체 전 참조 grep 증빙, 항목별 커밋 `feat(W3):`. 큰따옴표 금지. `node --check` + `npm test`.
5. 보고서: `docs/이본지층/W3_구현보고-260716.md`.

## 작업 항목

### W3-1 CLAUDE.md §4 용어 정정 (문서의 심장)
- `공명 (resonance)` 항목: "트루엔딩 도달. 궤적 브릿지 생성 트리거" → **"접촉 — 여정 중 유령과 정말 겹치는 드문 사건 (회차당 최대 1회 표시). 승리 상태 아님. 인기도/좋아요 아님"**.
- `trajectory bridge` 항목: "공명 엔딩 도달자의 궤적이 자동 변환된 브릿지" → **"접촉 기록 (2026-07-16 전용) — 접촉 순간의 발화·씬·시각. 정본화 없음"**.
- §6.3 별이엔진 출력 설명 등에서 트루엔딩 언급 있으면 같은 방향으로. **엔진 원칙("판단하지 않는다")은 그대로** — 오히려 이번 재정의가 그 원칙을 완성한다는 주석 한 줄 허용.

### W3-2 문서 스윕
- 대상: `docs/별이엔진_V4-*.md`, `docs/TEM_시스템_매뉴얼-260410.md`, `docs/데모기억_풀기능_체크리스트-260707.md`, 그 외 grep `트루엔딩|true ending|공명 엔딩` 전수.
- 방식: 역사 문서(회의록·폐기 기록)는 **원문 보존 + 상단에 한 줄 주석**("공명 정의는 260716 이본지층 설계로 대체"). 현행 규범 문서(매뉴얼·체크리스트)는 **본문 수정**.
- 산출: 수정한 문서 목록 + 보존 처리한 문서 목록.

### W3-3 셸 어휘 은퇴
- `js/app/endScreen.js`: isTrueEnding 분기·trueEndingBadge·"You reached the true ending" 류를 중립 문구(이본 언어)로 교체 또는 도달 불가 확증 시 해당 블록 제거. **파일 전체 은퇴는 금지** (comparison/live 생사 미확정 — R5 보류 사유 유지). 어휘만.
- `js/lib/i18n.js` + `js/npc-dialogues.js`(NPC_DIALOGUES.archive.trueEnding 등) + index.html: `true.?ending|트루` grep 전수 → 교체.
- 교체 문구 결: "당신은 ~한 판본이 되었다"의 언어. 번역 어색하면 임시 중립 문구 + 보고서에 "작가 감수 필요" 표기.

### W3-4 접촉 희소성 측정 (결정 3의 선행 과제)
- 스크립트 `tools/contact_rate_probe.mjs` (오프라인, DB SELECT만):
  - 기억별 ghost_variants 로드 → anchor(is_seed & parent null) 대 나머지 변주의 코사인 분포 → 0.85/0.90/0.92 초과 비율 표.
  - utterances(잔상 8행)·plays의 user_emotion으로 실입력 대 anchor 유사도 분포도 가능하면 병행 (데이터 부족하면 부족하다고 기록).
- 산출: `docs/이본지층/접촉희소성_측정-260716.md` — "현 0.85에서 접촉은 대략 몇 턴에 한 번 꼴" 추정 + **임계 권고안** (0.85 유지 / 0.90 / 0.92 중, 근거와 함께). 최종 결정은 지휘·작가.

### W3-5 (선택, 시간 남으면) 어휘 잔재 지도
- js/ui·js/core·play-test·supabase 쪽 `트루|true.?ending|resonan` 잔재를 **수정 없이** 목록화 → W4 배선 참고자료.

## 하지 말 것
- 기능 추가·연출 구현 (W4), 임계값 실제 변경 (측정과 권고까지만), endScreen 파일 삭제, dialog/play-test의 resonance 분류 로직 수정 (그건 어휘가 아니라 기능이며 W4 소관).

## 보고서 양식
항목별 교체 전/후 인용(대표 3~5개)·커밋 해시·grep 증빙, 측정 보고서 링크와 임계 권고 한 줄. "못 한 것 + 작가 감수 필요 목록" 필수.
