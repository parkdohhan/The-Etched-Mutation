# W1 구현보고 — 이본 지층 코어 엔진 (2026-07-16)

> 레인: W1 지형 코어. 정본: `docs/이본지층/이본지층_설계_v1-260716.md` (§3 작용·§4 위상 자·§6 계약).
> 핸드아웃: `docs/이본지층/핸드아웃_W1_지형코어-260716.md`.
> 완료 조건 = 시뮬레이터에서 **가짜 관객 500명 리트머스 통과**. → **통과.**

---

## 0. 한 줄 요약

작가 바닥(제0판) + 기억 작용 3종(강화·망각·재응고) + 위상 자를 가진 결정론 지형 엔진을 신설하고,
발자국·당신에게 **실데이터**로 500명 리트머스 5종을 **전부 통과**시켰다. 플래그 `tem_variant_strata`
**기본 OFF** — 켜기 전엔 어떤 기존 경로도 변하지 않는다. `npm test` **564 → 581**(신규 17), 회귀 0.

---

## 1. 절대 규칙 준수 체크

| 규칙 | 준수 |
|---|---|
| 내 구역만 (`js/shared/**` 신규+훅, 시뮬 페이지, `js/terrain-*-sim-page.js`, `test/unit/`) | ✅ |
| play-test·admin·supabase·js/app·js/ui 금지 (배선은 W4) | ✅ 손 안 댐 |
| 브라우저 = 시뮬 페이지 한정 (play-test 안 엶) | ✅ `terrain-variant-sim.html`만 |
| 배포·마이그레이션·DB 쓰기 금지 / DB SELECT 허용 | ✅ SELECT 2건(바닥 재료 실측)만 |
| 커밋 `feat(W1):` | ✅ |
| `npm test` 회귀(564) + 신규 | ✅ 581 (564+17) |
| 플래그 기본 OFF | ✅ |

---

## 2. 산출물 (파일별 수리 전/후)

| 파일 | 전 | 후 |
|---|---|---|
| `js/shared/tem_variant_strata.js` | 없음 | **신규** 엔진 본체(≈640줄). §6 API 전부. 물리(물방울·비탈무너짐·씨앗난수·simplex) 복사 이식 |
| `js/shared/tem_af_strata_terrain.js` | replay-terrain 훅만 | **입구 훅 1블록 추가** (variant strata, 플래그 OFF→원본 그대로, 에러→원본 폴백) |
| `test/unit/tem_variant_strata.test.js` | 없음 | **신규** 17개 (결정론·위상 자·질량 로그·저장 왕복·복구) |
| `terrain-variant-sim.html` | 없음 | **신규** 시뮬 페이지 셸 |
| `js/terrain-variant-sim-page.js` | 없음 | **신규** 500명 리트머스 오케스트레이션 + 그래프 + 스냅샷 측정 |
| `docs/이본지층/W1_구현보고-260716.md` | 없음 | **신규** (이 문서) |

원본 파일 무수정 확인: `tem_replay_terrain.js` 손 안 댐(복사 이식만). 은퇴는 W4 몫.

---

## 3. 항목별 구현 (W1-1 ~ W1-5)

### W1-1 모듈 뼈대 + 바닥(제0판)
- `buildBase(memoryData, opt)` — 결정론 연속 대륙. 씬 감정 분포(안 뭉갠 17축)가 자기 로브에 융기,
  패치 중심(닻) = `meta.stage_position`, 없으면 감정 VA 투영 폴백.
- 물방울 침식(§9.4 특수화 3종: 태어나는 자리=전이 경로, 초기 방향=이동 벡터, 순서=재생 순서)·
  비탈 무너짐·씨앗 난수·simplex **복사 이식**. 섬/공허/조각 배치 코드는 안 가져옴(연속 대륙).
- **결정론 못박음**: 단위 테스트 `같은 memoryData → 바닥 RMS 차이 = 0` 통과.
- **실데이터 바닥**: 발자국(6씬) 실 `original_emotion` + `stage_position` 으로 바닥 1회 생성.
  → `W1_스크린샷/바닥_발자국.png` (features=6 = 씬 6개 봉우리).

### W1-2 발길 지도 + 작용 3종 + 질량 로그
- 발길 지도(높이맵과 같은 격자). 물 지난 칸 누적, 저장 시 0~1 정규화.
- **강화**: 침식력 `× min(2, 1 + k_reinforce·발길)` (dropOne 안에서 셀별).
- **망각**: `lerp(높이, 이웃8평균, k_forget·(1−발길))`, **현재 땅**에 적용.
- **재응고**: 이웃 높이차 > τ_rough 칸만 국소 무너짐(질량 보존).
- **질량 로그(§9.6)**: 침식·재응고 전후 흙 총량 보존 검증(허용오차). 망각은 lerp라 예외 — `forgetDrift` 별도 표기.
- **파이프라인 순서 고정**: ①침식(강화) → ②망각 → ③재응고 → ④불변량 → serialize.

### W1-3 위상 자
- union-find 서브레벨 필트레이션. 지속도 ≥ p_min 봉우리(초레벨)·분지(서브레벨) 개수.
  `peaks = 극값(−field)` (전역 최고봉 포함) + `basins = 극값(field, 전역 '바다' 제외)`.
- `getInvariants() → {features, peaks, basins, relief, roughness, mass}`.
- **단위 테스트**: 합성 봉우리 3개 → `features=3` (peaks=3, basins=0), 잡음 추가해도 불변. 통과.

### W1-4 시뮬레이터 + 500명 리트머스
- 독립 페이지. 발자국 바닥 → 가짜 관객 500명(여정 프리셋 5종 랜덤 혼합, 씨앗 고정) 순차 봉인 →
  매 봉인 불변량·RMS 기록. 리트머스 5종 그래프 + 스냅샷 크기 실측.
- 결과: **§5 리트머스 표** 참조. 전부 통과.

### W1-5 복구 도구
- `rebuildFromPlays(playRows)` — (user_id, 30분 간격) 세션 군집 → 시간순 베이트 근사 → 전체 재굽기.
  반환값에 `approximate:true` + note 명시. 단위 테스트 2개(세션 3분할·결정론) 통과.

---

## 4. §6 레인 계약 준수 (동결 API)

`window.TemVariantStrata` 노출:
`isEnabled`/`setEnabled`, `buildBase`, `beginRun`/`beat`, `applyVisitorErosion`,
`loadLayer`/`serializeLayer`, `rebuildFromPlays`, `getInvariants`, `computeFields`(입구 훅용).
플래그 키 `tem_variant_strata` (URL `?variant_strata=1/0`, localStorage). **옛 `tem_replay_terrain` 키 안 읽음.**
→ 계약 그대로. **변경 없음.**

---

## 5. 리트머스 5종 결과 (발자국, G=96, 500명, 씨앗 고정)

그래프 = `W1_스크린샷/500명후_발자국.png` 하단 패널(①features 밴드+선, ②RMS 곡선, ③④⑤ 요약).

| # | 리트머스 | 기준 | 실측 | 판정 |
|---|---|---|---|---|
| ① | features 밴드 유지 | 바닥 6 ±60% = **[2,10]** | 최종 **4** (경로 4~7, 이탈 0) | ✅ |
| ② | 바닥과 RMS 차이 유의(이본 발생) | > 0 유의 | **1.126** (바닥 std 1.422의 79%) | ✅ |
| ③ | 셔플 리트머스(순서 섞으면 다른 땅) | > std×0.05 = 0.071 | **0.091** | ✅ |
| ④ | 발자국·당신에게 서로 다른 땅 | 유의 | **0.892** | ✅ |
| ⑤ | 질량 로그 이상 | 0 | **0** | ✅ |

- **features 곡선의 의미**: 6에서 시작 → 관객 ~70~200 구간 **7로 상승**(침식이 새 분지를 팜) → 서서히 6·5·4.
  "원본에서 무한히 멀어지되(rms 79%) 무형태로는 안 감(features 4, 밴드 안)"이 실증됨.
- **질량**: 바닥 1702 → 500명 후 1699. 침식·재응고는 보존, 미세 감소는 망각(lerp) 몫(로그에 별도).

---

## 6. 손잡이 최종값 + 근거 (밴드 이탈 → 조정 기록)

**초기값으론 밴드를 이탈했다.** 설계 초안값(k_forget 0.005, τ=평균×3, 원본 물리)으로 500명 돌리니
**features 6 → 1 붕괴**(rms 1.41 ≈ 바닥 std — 원본을 통째로 갈아엎음). 핸드아웃 W1-4 지시대로
손잡이/물리를 조정하고 근거를 남긴다.

### 6.1 진단 (스윕으로 범인 격리)
- erosion_scale 0.5→0.15 낮춰도 rms 1.41→1.39 **거의 불변** → 물방울 개수는 주범 아님.
- 각 패스 격리: **물방울(erosion_scale=1) 단독 → features 1**, 망각 단독 → 3, "거의 아무것도 안 함" → 5.
- 핵심 통찰: **순수 침식은 복원력이 없어(우상향 융기 없음) 어떤 비율이든 500번이면 평탄 끌개로 수렴.**
  비율은 *속도*만 정하고 *끝점*(평탄)은 같다 → 관객 예산(500) 안에서 살아남으려면 관객당 침식이 얇아야 함.
- 부차 범인 2개: (a) 전이마다 돌던 전역 `thermal`(500명×~5전이×2 = ~5000회 확산) — 1회 굽기용 잔재,
  (b) τ_rough=평균×3=0.64 가 성긴 봉우리 경사(~3)보다 낮아 재응고가 매 봉인 봉우리를 먹음.

### 6.2 최종값 + 근거
| 손잡이/상수 | 초안 | 확정 | 근거 |
|---|---|---|---|
| `k_reinforce` | 0.6 | **0.6** | 유지 |
| `k_forget` | 0.005 | **0.0008** | 500 봉인 누적 시 잔주름만 지우고 실루엣 보존 |
| `tau_rough_mult` | 3 (평균 기준) | **1.0 (p98 기준)** | τ 기준을 '평균 이웃차'→'이웃차 p98'로 변경. 평균은 매끈한 대륙에 눌려 봉우리를 먹음. p98(≈1.57)은 바닥 자연 경사 위 → 침식이 새로 판 절벽만 굳힘 (§3-3 "모래는 절벽 불가" 원의미에 더 충실) |
| `erosion_scale` (신규 §9.5) | — | **0.012** | 봉인당 물방울 총량 배율. 관객당 침식을 얇게 해 500명이 밴드 안(features 4~7)에서 이본화하되 무형태로 안 가게 하는 창 |
| 전역 `thermal` | 전이마다 2회 | **폐기** | 누적 모델에서 500× 확산 = 평탄화. 비탈 무너짐은 §3 ③재응고(1회/봉인)만 |
| `PHYS.erode/deposit/capacity/maxSteps` | 0.35/0.3/6/80 | **0.06/0.10/4/48** | 원본은 1회 굽기용. 누적 모델용으로 물방울 하나를 얕게(§9.5 "1회 보정 후 고정") |

전부 `TemVariantStrata.HANDLES` + `PHYS` 상단 주석에 근거 기록. 손잡이는 시뮬 슬라이더로 실시간 조정 가능.

---

## 7. 스냅샷 직렬화 크기 실측 (목표 <150KB/장)

| 대상 | G=96 실측 | G=160 외삽(strata 뷰 해상도) | 목표 |
|---|---|---|---|
| **높이맵 스냅샷** (§4·§5 Storage, '장') | **47.4 KB** | **131.7 KB** | <150KB ✅ 둘 다 |
| 변형층 (terrain_layers: height_delta+foot_map) | 251.4 KB | (외삽 ~698 KB) | — |

- '장'(높이맵 스냅샷)은 목표 충족. G=160(입구 훅 기본 해상도)에서도 131.7KB로 여유.
- **변형층은 W2 저장 시 희소/타입드 인코딩 필요** — height_delta는 대부분 셀이 ≈0(안 변한 대륙)이라
  2자리 반올림 JSON은 낭비. base64(Float32) 또는 sparse(변한 셀만)로 대폭 축소 가능. **W2에 제안(계약 밖)**.

---

## 8. 바닥 / 500명 후 스크린샷 비교

- `docs/이본지층/W1_스크린샷/바닥_발자국.png` — 제0판. 6씬이 각자 stage_position 자리에 봉우리(features=6).
- `docs/이본지층/W1_스크린샷/500명후_발자국.png` — 500명 후. 봉우리 침식·병합(4개) + 물길·능선 퇴적.
  변형됐지만 형태를 지킴. HUD·리트머스 그래프 동봉.

---

## 9. 테스트 결과

- `npm test`: **581 passed (23 files)** — 기준선 564 + 신규 17, 회귀 0.
- 신규 17개: 계약 노출·플래그 OFF / 결정론 바닥·다른기억·유한값·폴백 / 위상 자 봉우리3·잡음불변·1봉우리·형상 /
  파이프라인 질량보존·delta비영·셔플·features밴드 / 저장 왕복 / 복구 세션3분할·결정론.

---

## 10. 설계 계약 해석·편차 (정직 기록)

1. **buildBase 재료** — 정본 §2#5는 "바닥 = 작가 씬 감정 + stage_position + 첫 서술 순서 → 결정론,
   plays/오염 불참"으로 못박음. 핸드아웃 W1-1 문구 "기존 `computeAfTerrainFields` 결과를 출발점"은
   더 느슨(그 함수는 plays/오염 의존 → 결정론 깨짐). **정본 우선** — 자족 결정론 바닥을 만들되
   필드 투영 레시피(VAD·앵커·감정색)는 같은 정신으로 재사용. **계약(buildBase 시그니처) 불변.**
2. **τ_rough 기준 변경** — 핸드아웃 "평균×3" → "p98×1.0". §6.1 근거. API 아님.
3. **전역 thermal 폐기 / PHYS·k_forget·erosion_scale 보정** — §6.2. 전부 §9.5 "1회 보정" 범위 + W1-4 "손잡이 조정" 지시.
4. **위상 자 features 정의** — peaks(전역 최고봉 포함) + basins(전역 '바다' 제외). 합성 3봉우리에서 정확히 3.
   설계 §4 "봉우리·분지 개수"를 이 규약으로 구현. 근거는 코드 주석.

---

## 11. 못 한 것 (필수 기록)

1. **위상 보장의 한계** — "무형태로 못 감"은 **보정된 침식률·관객 예산(500) 안에서만** 성립.
   순수 침식은 단조 평탄화이므로 *무한* 관객이면 결국 features→1로 간다. 진짜 무한 보장엔
   복원항(퇴적 융기)이나 관객당 침식 상한이 필요. **10월 튜닝 과제**(설계 §8 재응고×오염 연결과 함께).
2. **입구 훅은 존재·안전만** — 플래그 ON 시 바닥+로드된 delta 렌더까지. 실제 run/봉인 배선(play-test)은 W4.
   훅은 default OFF·에러 폴백만 검증(브라우저 = 시뮬 페이지 한정 규칙 준수, play-test 안 엶).
3. **비정규 감정축** — 발자국 실데이터에 canonical 17축 밖 `emptiness` 존재. 엔진은 무시(안전).
   17축 통일은 별도 대기(설계 §8).
4. **변형층 저장 인코딩** — 251KB(G=96). W2 희소/타입드 인코딩 제안만(계약 밖, DB 쓰기 금지).
5. **당신에게 바닥 스크린샷** — 리트머스 ④ 수치(RMS 0.892)로만 검증. 별도 스크린샷은 생략(발자국 중심).

---

## 12. 커밋

- `feat(W1):` — 엔진 본체 + 입구 훅 + 시뮬 페이지 + 단위 테스트 17 + 이 보고서 + 스크린샷 2.

### 12.1 커밋 해시
- `b202a9e` — feat(W1): 이본 지층 코어 엔진 — 작가 바닥 + 작용 3종 + 위상 자 + 500명 리트머스
  (엔진·훅·시뮬·테스트·보고서·스크린샷 일괄). 활성 커밋 훅 없음(husky/git hooks/lint-staged 부재) 확인.
- 배포 버전/백업 경로: 해당 없음(이 레인은 배포·마이그레이션 금지 — 엔진+시뮬만).
