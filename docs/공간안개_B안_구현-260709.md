# 공간 안개 B안 구현 — 260709

> "기억을 안개 속에서 헤쳐나간다" — 지점별 안개 마스크 + 회랑 + 도착 개화 + 소프트 이동 차단.
> test/fog-reveal-test.html 검증분(260708 커밋 f6aeba8)의 본 게임 이식.

## 스위치

- **flag**: `?spatial_fog=1` (URL, sticky → localStorage `tem_spatial_fog`) / 끄기 `?spatial_fog=0`
- 기본 OFF. 꺼져 있으면 셰이더 분기·모듈·훅 전부 무동작 — 기존 플레이 영향 0.

## 구성 (3파일)

| 파일 | 역할 |
|---|---|
| `js/shared/tem_af_strata_terrain.js` | 셰이더 마스크: `_sfPatchShader` — fog_fragment 를 "걷힌 자리 목록 밖 = 짙은 안개(하늘회청 0x555f6e, 밀도 0.055)"로 교체. 가장자리 fbm 일렁임. 본지형+퀼트 조각 재질 양쪽. 공유 uniform `window.__temSpatialFogUniforms` (재구축돼도 같은 객체 재바인딩). `_fpTick` 이동에 `__temSpatialFogConstrain` 훅. |
| `js/ui/lumen_spatial_fog.js` (신규) | 목록 관리인. 원(지역, 3초 개화)·선분(회랑, 1.6초 파임) 목록 + 애니메이션 + 매 프레임 uniform 주입(render wrap). `revealAt(x,z)` 0..1 판정 — 시각·게이트·이동·미니맵이 전부 이 하나를 공유. 게이트(유령 숨김/페이드), 소프트 이동 차단, 도착 개화(`openOnArrive`, 트리거 7m). |
| `play-test.html` | 진입 attach + 게이트 등록(마네킨 root + 혼잣말 sprite) + 입장 시드 + 턴 연결 + 미니맵 필터 + trace line 생략. |

## 연출 규칙 (사용자 결정 260709)

1. **입장**: 스폰 주변(반경 14)만 걷힘 + 접근 가능 핀마다 회랑(반폭 5)이 파임.
   지역은 **도착해야 개화** (플레이어가 핀 7m 안 → 반경 16 원이 3초 개화).
   가 본(visited) 핀 지역은 즉시 걷힌 채 시작.
2. **턴**: 씬 완료 → 맵 복귀 시 새로 열린 핀마다 현재 위치에서 회랑 + 도착 개화.
   기존 3D trace line 은 안개 ON 이면 생략 (회랑이 길잡이).
3. **이동**: 소프트 차단 — 걷힘 0.35 아래부터 감속, 0에서 정지("아무리 걸어도 나아가지지 않는다").
   더 맑은 쪽 걸음은 항상 통과 (갇힘 방지).
4. **막다른 곳**: 남은 접근 핀 0 → `liftAll()` — 안개 전부 걷혀 지나온 길이 드러남 (설계 원칙 4).
5. **유령**: 안개 속 = `visible:false` (클릭 레이도 자동 차단). 개화 중 = opacity 비례 등장.
   접근 가능 유령은 회랑 끝에 항상 희미하게 보임 — 목적지 유도 (의도).
6. **미니맵**: 걷힘 0.5 미만 핀은 점 안 찍음 (원칙 5 — 플레이어는 안개를 본다).

## 궤적 원과의 관계

궤적 엔진(어떤 핀이 열리나, 감정 공간의 원)은 **한 글자도 안 건드림**.
안개는 엔진 출력(accessiblePinIds 차집합)을 소비하는 무대 연출 레이어.

## 남은 것 (4단계)

- [ ] replay fog(전역 밀도)와 공존 정리 — 둘 다 켜면 이중 안개. 상호 배타 권장.
- [ ] 감각 튜닝: 안개색·개화 속도·회랑 폭·소프트 차단 감속 곡선 (실플레이 눈 확인)
- [ ] MAX_REVEAL 24 초과 런(씬 ~12개+) — 오래된 자리 재안개화 허용 or 병합
- [ ] 지평선 경계(안개 지형 실루엣 vs 어두운 하늘) 톤 정리
- [ ] 씬 소리(lumen_audio_space)를 안개에 게이팅할지 — 현재는 안개 속 소리 들림(귀 길찾기, 의도적 보류)

## 롤백

flag OFF = 즉시 무력화. 완전 제거 = 신규 파일 삭제 + 두 파일의 `ADDITIVE 공간 안개` 표시 블록 삭제.
