# 수리 핸드아웃 R5 — 앱 셸 위생 (index.html + js/app 죽은 코드 은퇴)

> Phase C 수리 5레인 중 R5. 판정 근거: docs/점검/PhaseB_마스터취합-260714.md (줄기 E) + L2_발견-260713.md.
> 미션: **확증된 죽은 코드를 은퇴시키고 셸의 자잘한 어긋남을 닦는다.** "고쳤는데 안 바뀐다"의 온상 제거.
> **주의: Record 기능 흐름(recordChat·confession 본문)은 이번 라운드 제외** — 사용자 결정. 아래 명시된 지점만.

## 절대 규칙

1. **내 파일만 수정**: `index.html`, `js/index.js`, `js/app/**`, `js/expInterview.js`. play-test/admin/supabase/js/ui/js/core/js/shared 금지.
2. **삭제 원칙**: 삭제 전 반드시 참조 0건을 grep 으로 확증하고, 그 grep 결과를 보고서에 남긴다. 삭제는 **항목별 별도 커밋** (롤백 단위).
3. 배포·마이그레이션·DB 쓰기·브라우저 금지. `node --check` + `npm test` 로 검증.
4. 커밋 접두어 `fix(R5):`. PowerShell 커밋 메시지 큰따옴표 금지.
5. 보고서: **`docs/점검/R5_수리보고-260714.md`**.

## 수리 항목

### R5-1 (L2-08 🟡) live.js 3,080줄이 첫 로드에 전량 포함되는 문제
- `js/app/confession.js:19` 의 정적 `import { resetLiveState, switchGeneratedTab, startVoiceWaveLiveAnimation } from './live.js'` 를 **동적 import 로 전환** (사용처는 startRitualFlow 계열뿐 — 그 함수 안에서 `await import('./live.js')`). confession.js 는 이 import 줄과 해당 사용처만 만질 것 (Record 본문 금지).
- 완료 기준: index.js 최초 로드 체인에서 live.js 가 빠짐 (grep 으로 정적 import 0건 확인).

### R5-2 (L2-06 🟡) expInterview.js 820줄 은퇴
- 대상 DOM 이 index.html 에 0개, 호출부는 archive.js:1616(그 자체 죽음)과 live.js:1740(휴면)뿐임을 재확증 후: 파일 삭제 + import 2곳 제거. live.js 쪽은 import 줄만 제거(본문 무수정).

### R5-3 (L2-11 🟡) registration 죽은 덩어리
- `js/app/registration.js`(505줄) 삭제, `bindEvents.js:324-461` 의 bindMemoryRegistrationEvents 블록 제거, index.html:589-697 의 등록 화면 HTML 제거. window 핸들러 5개 미존재 재확증 후.

### R5-4 (L2-12 🟡) archiveEntry.js 삭제
- import 0건 재확증 후 파일 삭제. `#archiveEntryContainer` DOM 은 enterArchive 가 숨김 처리에 쓰면 유지, 아니면 함께 제거.

### R5-5 (L2-07 🟡, 신중) archive.js 죽은 절반 + contaminationPresenter
- `selectMemory` 가 play-test 로 리다이렉트하므로 index.html 내 씬 플레이 함수들(renderScene/renderChoices/runEngineStep/…)이 도달 불가. **함수 단위로 참조 0건 확증된 것만** 삭제, 별도 커밋. 확신 없는 건 남기고 보고서에 "의심 잔존" 표기. `contaminationPresenter.js`(248줄)는 유일 소비자가 그 죽은 구역이면 함께 삭제.
- 주의: archive.js 의 목록/검색/입장 로직은 살아있다 — 씬 플레이 구역만.

### R5-6 (L2-09 🟢) RECORD 메뉴 설명 복구
- index.html:190-192 주석 안의 `description-text`(data-for="record") 주석 해제. RECORD 호버 시 설명이 뜨는지 구조 확인.

### R5-7 (L2-10 🟢) 세션 기록 유령 쿼리
- `#sessionHistoryList` DOM 이 없어 결과가 버려지는 `loadSessionHistoryFromDB()` 호출 제거 (함수 자체는 보존 — 나중에 DOM 복원 시 재사용). `#displayLoginMethod` 죽은 마크업은 제거 또는 값 채우는 한 줄 추가 중 택1.

### R5-8 (L2-18 🟢) updateStrata 제거
- `js/index.js:360` — 대상 DOM·호출부 0. 삭제.

### R5-9 (E1 🟡) index.html 캐시버스터
- index.html 의 자체 js script 태그(모듈 엔트리 포함)에 `?v=260714` 통일.

### R5-10 (L2-17 🟢, 선택) i18n 최소 수선
- `endScreen.js` 엔딩 문구 영어 하드코딩 → getCurrentLanguage 분기(ko 문구는 기존 톤 유지, 번역 어색하면 보고만). `auth.js:146,161` 의 ko/en 혼합 한 줄 정리. **confession/recordChat 내부는 금지** (Record 제외 원칙).

## 보고서 양식
항목별: 삭제/수정 줄 수, 참조 0건 grep 증빙, 커밋 해시, node --check·npm test 결과. "삭제 보류(의심 잔존)" 목록 필수.
