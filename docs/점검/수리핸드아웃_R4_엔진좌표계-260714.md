# 수리 핸드아웃 R4 — 엔진 좌표계·테스트 (js/shared + 엔진)

> Phase C 수리 5레인 중 R4. 판정 근거: docs/점검/PhaseB_마스터취합-260714.md (줄기 C + E 일부) + L5_발견-260713.md.
> 미션: **판단 좌표계의 자잘한 균열은 코드로 고치고, 큰 균열(12↔17축)은 설계 제안서로 만든다.** 축 매핑은 작품 판단이라 코드보다 결정이 먼저다 (비전 우선 원칙).

## 절대 규칙

1. **내 파일만 수정**: `js/shared/**`, `js/core/ByeoriEngine.js`, `js/core/SceneNavigator.js`, `test/**`, `tests/**`, `vite.config.js`, 관련 docs.
2. **수정 금지 (다른 레인 소유)**: `js/core/ContaminationTracker.js`(R2), `js/core/SeekerFingerprint.js` — **특히 SeekerFingerprint 의 EMOTION_KEYS export 는 Tracker 가 재수출하므로 형태 변경 절대 금지.** `js/shared/ghost_presets.js` 삭제 금지(소비처가 R2 구역 — 보고만).
3. 배포·마이그레이션·DB 쓰기·브라우저 금지.
4. 커밋 접두어 `fix(R4):`. PowerShell 커밋 메시지 큰따옴표 금지.
5. 보고서: **`docs/점검/R4_수리보고-260714.md`**.

## 수리 항목 — 코드 (확정분)

### R4-1 (C2 🟡) flattenDeltas 22축 → 17축
- `js/shared/math.js:254-259`: 하드코딩 22키 기본값을 `DEFAULT_EMOTION_ANCHORS`(17축) 재사용으로 교체. 유령 키 `grief`/`tenderness` 소멸.
- 효과 고지: 12축 벡터가 유입되던 경우 longing/confusion/emptiness 가 shape 에서 빠진다 — **이건 버그가 아니라 17축 정본 정렬** (level 과 같은 좌표계가 됨). 보고서에 명시.
- `EXTENDED_EMOTION_ANCHORS`(math.js:29, 소비처 0) 죽은 상수 제거.
- math.test.js 갱신 (기존 assertion 이 22축 전제면 17축으로).

### R4-2 (C3 🟡) bucket 임계값 정본화
- 문서(V4 §11)·코드(math.js:364-381)·죽은 테스트가 3벌. **코드 값을 정본으로 채택** (프로덕션에서 돌던 값 — 동작 변경 없음). `docs/별이엔진_V4-궤적기반_정렬도-260327.md` §11 에 "확정: HIGH≥0.50 / LOW<0.10 / 히스테리시스 0.40·0.15 (2026-07-14, 코드 기준 정본화)" 추가.

### R4-3 (L5-10 🟡) 테스트 재편 — V4 를 살리고 화석을 치운다
- 루트 `tests/byeori_v4_scoring.test.js`(현행 설계 테스트인데 실행 안 됨) → `test/unit/` 로 이동, vitest 로 돌게 조정. **깨지면 고치지 말고 실패 내용을 보고서에** (엔진 v4 와 테스트 중 누가 옳은지는 지휘 판정).
- `test/unit/byeori_v2_scoring.test.js`·`byeori_v3_scoring.test.js`(exclude 된 화석) 삭제 + vite.config exclude 정리.
- 루트 `tests/` 잔여 파일 처분(현행이면 이동, 폐기 설계면 삭제) — 각 파일 판정 근거를 보고서에.

### R4-4 (L5-12 🟢) 콘솔 오염
- `tem_geo_map.js:38-42` 4줄 + `math.js:10` 1줄 무조건 출력 제거(또는 debug 플래그 뒤로).

## 수리 항목 — 설계 제안서 (코드 금지)

### R4-5 (C1 🔴, 제안서만) 12축↔17축 통합안
- 산출물: **`docs/감정축_통일_제안_v1-260714.md`**. 코드 수정 금지 — 축 매핑은 기억유전학 정본 결정이 필요한 작품 판단.
- 담을 것: ① 현황 표(L5 별첨 B 재정리 — 생산 8/12축, 판단 17축, 교집합 9축) ② 통합안 3안 비교: (A) 생산측을 17축으로 확장(Edge 프롬프트 + admin, 기존 데이터 마이그레이션 불요) / (B) 12→17 매핑 함수(longing·confusion·emptiness 를 어느 축으로 보낼지 매핑 표 초안 포함) / (C) 17축에 3축 추가한 20축 신정본 ③ 각 안의 변경 파일·데이터 마이그레이션·롤백 난이도 ④ 권고 1안.

### R4-6 (L5-07·08 🟡, 제안서에 포함) avoidance·contradiction 기하
- avoidance 빈 벡터(사실상 배열 첫 씬 선택)와 `_negateEmotion` 보수(complement, 균일 벡터화) 문제의 대안 설계를 위 제안서에 §로 추가 (예: avoidance = 저강도·중립 앵커 벡터, contradiction = 지배 축 반전+정규화). **SceneNavigator 코드는 이번에 건드리지 말 것** — 서사 동작이 바뀌는 사안.

## 보고서 양식
항목별: 수리 전/후, 커밋 해시, npm test 결과(총 개수 변화 포함 — V4 테스트 편입으로 수가 는다). R4-3 에서 V4 테스트가 실패한 항목은 전부 원문 인용. "못 고친 것" 섹션 필수.
