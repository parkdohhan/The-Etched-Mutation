# R5 수리 보고 — 앱 셸 위생 (2026-07-14)

> ⚠️ [260716 이본지층] 본문이 "죽었을 가능성이 큰 코드라 번역 낭비"로 보류했던 endScreen.js 트루엔딩 문구는, 260716 W3에서 위계 어휘 철거 관점으로 실제 교체됨 (접촉 = 승리 아닌 드문 사건). 정본: docs/이본지층/이본지층_설계_v1-260716.md

> 핸드아웃: `docs/점검/수리핸드아웃_R5_앱셸위생-260714.md`. Phase C 수리 5레인 중 R5.
> 미션: 확증된 죽은 코드를 은퇴시키고 셸의 어긋남을 닦는다 — "고쳤는데 안 바뀐다"의 온상 제거.
> 원칙 준수: 내 파일만 수정(`index.html` · `js/index.js` · `js/app/**` · `js/expInterview.js`),
> Record 흐름(recordChat·confession 본문) 무수정, 배포·DB·브라우저 0, 항목별 별도 커밋.

## 한 줄 결과

**2,603줄을 걷어냈다** (삽입 74줄). 첫 로드 번들에서 live.js 3,080줄이 빠졌고, 죽은 화면 3개(씬 플레이·기억 등록·체험 인터뷰)가 사라졌다. 남긴 것 2건은 "의심 잔존"으로 아래에 명시했다 — 둘 다 **R2(오염복원) 레인의 소관과 겹쳐서** 내 판단으로 지우지 않았다.

## 커밋 (9개, 항목별 롤백 단위)

| 커밋 | 항목 | 내용 | 증감 |
|---|---|---|---|
| `96d043a` | R5-1 | live.js 정적 → 동적 import | 첫 로드에서 3,080줄 제외 |
| `eefe51b` | R5-2 | expInterview.js 은퇴 | −820 |
| `fb1b243` | R5-3 | memory registration 덩어리 제거 | −754 |
| `841999e` | R5-4 | archiveEntry.js 삭제 | −253 |
| `eebac0a` | R5-7 | 세션 기록 유령 쿼리 제거 | 헛쿼리 1건/진입 제거 |
| `68108a6` | R5-8 | updateStrata 제거 | −1 |
| `c722b2e` | R5-6+R5-9 | RECORD 설명 복구 + 캐시버스터 통일 | 자산 10개 `?v=260714` |
| `d0baae5` | R5-5 | archive.js 씬 플레이 구역 제거 | −726 |
| `8d9b60d` | R5-5b | 후속 미사용 import 정리 | −5 |

**검증**: 매 커밋마다 `node --check` 통과. 최종 `npm test` **564/564 통과 (22 파일)**.

---

## 항목별 상세

### R5-1 🟡 live.js 3,080줄이 첫 로드에 실리던 문제 — 해결

`confession.js:19` 의 정적 `import ... from './live.js'` 를 지연 로더로 전환했다.

```js
let _liveModule = null;
async function _loadLive() {
    if (!_liveModule) _liveModule = await import('./live.js');
    return _liveModule;
}
```

- 사용처는 Ritual 흐름 2개 함수뿐 (`startRitualFlow`, `saveRitualScene` — 둘 다 이미 `async`) → 그 안에서 `await _loadLive()`.
- 호출 4곳 치환: `resetLiveState` ×2, `switchGeneratedTab`, `startVoiceWaveLiveAnimation`.
- **완료 기준 충족**: `grep -rn "from './live.js'" js/app/` → **0건**. index.js 는 이미 동적(`js/index.js:85`).
- Record 본문 무수정 (원칙 준수).

### R5-2 🟡 expInterview.js (820줄) 은퇴 — 완료

**참조 0건 증빙** (worktree 사본 제외):

| 필요 DOM | index.html 내 개수 |
|---|---|
| `#sceneViewer` / `.scene-main` / `.scene-content` | 0 / 0 / 0 |
| `#choicesContainer` / `#free-input-container` / `#expInterviewZone` | 0 / 0 / 0 |
| `test/` 참조 | 0 |

붙을 자리가 없으니 호출돼도 `console.error` 후 흐름이 **멈춘다**. 씬 플레이는 play-test.html 로 이전됐다.

- `js/expInterview.js` 삭제, index.html 의 `<script>` + `css/expInterview.css` link 제거 (css 파일 자체는 보존).
- `archive.js` makeChoice 의 죽은 `typeof startExpInterview` 분기 제거 → `proceedToNextScene()` 직행 (기존 else 와 동일 동작).
- `live.js` 는 `window.startExpInterview` 가드라 **무수정** — 가드가 폴백 처리, DORMANT.

### R5-3 🟡 memory registration (754줄) 제거 — 완료

핵심: **등록 화면 HTML 은 있었는데 그걸 여는 버튼이 없었다.**

**참조 0건 증빙**:
- `#registerMemoryBtn` / `.register-memory-btn` → index.html 에 **0건** (진입 불가)
- `window.handleRegistrationInput` / `window.confirmScene` / `window.finishRegistration` → **할당 지점 0건**. bindEvents 의 가드가 항상 false.
- `window.startMemoryRegistration` 만 노출돼 있었으나 바인딩 대상 버튼이 부재.

제거: `js/app/registration.js`(505줄) · index.html 등록 화면(110줄) · `bindMemoryRegistrationEvents`(139줄) + 호출 + 죽은 버튼 바인딩 · index.js import 5개 + window 노출.
`bindConfessionEvents` 안의 `registerMemoryBtn` 조회는 **Record 소관이라 무수정** (null 가드, 무해).

### R5-4 🟡 archiveEntry.js (253줄) 삭제 — 완료

import 0건 (파일 자신의 `window.__archiveEntryDebug` 외 참조 없음).
`#archiveEntryContainer` DOM 은 `enterArchive` 가 숨김 처리에 쓰고 있어 **핸드아웃 지시대로 유지**했다 (채우는 코드가 없어 실질 no-op).

### R5-5 🟡 archive.js 씬 플레이 구역 (726줄) 제거 — 완료 (신중 항목)

`selectMemory()` 가 play-test.html 로 리다이렉트한 뒤로 이 코드는 **한 번도 실행된 적이 없다.**

**도달 불가 3중 확증:**

1. **DOM 0건** — `#sceneViewer` `#sceneText` `#choicesContainer` `#sceneNumber` `#emotionInput` `#waveCanvas` `#comparisonScreen` 전부 index.html 에 `grep -c = 0`.
2. **외부 호출부 0건** — 삭제한 14개 함수 전부 **오직 `js/index.js` 의 import 목록에만** 존재. window 노출 없음, onclick 없음. (`demoFlow.js` 의 `renderScene`/`SCENE_TRANSITION_MAP` 은 **자기 사본**이라 무관.)
3. **닫힌 순환** — `goToScene → renderScene → renderChoices → makeChoice → proceedToNextScene → renderScene`. 바깥에서 들어오는 입구가 없다.

내부 헬퍼(`getMemorySoundMap` · `ensureSoundscapeReady` · `quickAnalyze` · `_LIVE_KW` · `typeSceneText` · `buildArchiveSceneHTML` · `splitSentencesForScene` · `getArrivalTypingSpeed` · `SCENE_TRANSITION_MAP`)도 이 구역만 부르므로 함께 사망 → 같이 제거.

- 삭제: archive.js 1230~1932 + export 14개 + `contaminationPresenter` import
- 삭제: index.js 의 죽은 import 14개
- **보존**: `selectMemory` · `backToList` · 목록/검색/입장 로직 (살아있음)
- 후속(`8d9b60d`): 이제 import 줄에만 남은 `showEndScreen` · `NPC_DIALOGUES`/`getRandomDialogue` · `ByeoriEngine`/`byeoriEngine`/`sceneNavigator` · `createEmptyState` 제거
- **잔여 참조 grep 0건** 확인 완료

### R5-6 🟢 RECORD 메뉴 설명 복구 — 완료

메뉴 항목은 7-13(`a4013ca`)에 부활했는데 **설명문만 `<!-- hidden for pilot test phase -->` 주석에 갇혀** 있었다. 주석 해제.
배선 확인: `data-menu-id="record"` ↔ `data-for="record"` (`index.js:740` 이 hover 시 토글), i18n 키 `menu.desc.record` 는 **ko/en 양쪽 모두 존재** (`i18n.js:23`, `:149`).

### R5-7 🟢 세션 기록 유령 쿼리 제거 — 완료

`loadMypageDataFromDB` 가 마이페이지 진입마다 **세션 기록 50건을 조회**했지만, 그릴 자리 `#sessionHistoryList` 가 셸에 없어(`grep -c = 0`) 렌더러가 즉시 return → **응답이 통째로 버려졌다.**
`Promise.all` 에서 해당 쿼리와 렌더 호출만 제거. `loadSessionHistoryFromDB` / `renderSessionHistoryList` **함수 자체는 보존** (DOM 복원 시 재사용).

**핸드아웃 정정**: `#displayLoginMethod` 는 **죽은 마크업이 아니다.** `js/ui/UIManager.js:50-62` 가 `userData.loginMethod` 유무에 따라 표시/숨김 + `#loginMethodText` 를 채운다 → **유지**.

### R5-8 🟢 updateStrata 제거 — 완료

`#strataOriginal` / `#strataInterpretation` 이 셸에 없어(0건) 호출되면 첫 `getElementById` 에서 즉시 throw 하는 함수. 호출부도 0건.

### R5-9 🟡 캐시버스터 통일 — 완료

index.html 자체 자산 **10개**(js 8 + css 2)에 `?v=260714` 부여. 외부 CDN(supabase-js, three.js, 폰트)은 대상 외.

### R5-10 🟢 i18n 최소 수선 — **보류 (보고만)**

핸드아웃이 "선택" + "번역 어색하면 보고만" 으로 열어둔 항목. **도달성이 확정되지 않아 손대지 않았다.**

- `endScreen.js` 의 영어 하드코딩(`'Touching the Engraving'`, `'You reached the true ending.'` 등)은 실재한다. 그러나 `showEndScreen` 의 호출부는 R5-5 로 archive.js 쪽이 사라져, 지금 남은 경로는 **`comparison.js`(자체 도달성 미확증) 와 `live.js`(DORMANT)** 뿐이다. **죽었을 가능성이 큰 코드를 번역하는 건 낭비**라 판단.
- `auth.js` 의 ko/en 혼합(`'화자'`/`'체험자'`/`'진행중'` + 영문 라벨)은 `renderSessionHistoryList`(R5-7 로 확인된 **DOM 부재**)와 `showSessionDetail`(**살아있는 호출부 0건** — export 만 존재)에 있다. 같은 이유로 보류.

→ **지휘 세션 판단 요청**: comparison/endScreen 구역의 생사를 먼저 확정한 뒤 (별도 라운드) 번역할지 은퇴시킬지 정하는 게 맞다.

---

## 삭제 보류 (의심 잔존) — 필수 기록

### 1. `_applyContaminationAtEnd` + `_generateStageTextsInBackground` (archive.js, 약 155줄)

- **상태**: R5-5 로 호출부(proceedToNextScene / proceedToNextSceneOrEnd)가 사라져 **고아**가 됐다.
- **안 지운 이유**: 이 둘이 **오염 텍스트(text_stage_1/2/3) 자동 생성의 유일한 구현**이다. `contaminate-text` Edge Function 을 불러 DB에 캐시한다. 오염 복원은 **R2 레인의 미션**이고, R2 는 지금 `play-test.html` 과 `supabase/functions/contaminate-text` 를 수정 중이다(작업 트리에서 확인). 내가 지우면 R2 가 이식할 참조 구현이 사라진다.
- **부수 사실**: 이 코드가 index 셸의 죽은 구역에 있었다는 것은, **L3-11("5개 기억의 오염 텍스트가 전부 null")의 원인 규명**이기도 하다 — 생성기가 실행될 일이 아예 없었다.
- **R2 에게**: play-test 로 이식할지, 은퇴시킬지 결정 바람. 코드에 주석으로도 남겨뒀다.

### 2. `js/app/contaminationPresenter.js` (248줄)

- **상태**: 유일 소비자였던 `renderScene` 이 죽어 **고아**. archive.js 의 import 만 제거하고 **파일은 디스크에 보존**.
- **안 지운 이유**: 위와 같음 — 오염 = R2 레인. 이 파일은 셸의 **stage 텍스트 선택 로직**(어느 단계 텍스트를 보여줄지) 참조 구현이다. R2 가 play-test 의 리더를 고치는 중이라, 지금 지우면 대조군을 잃는다.

### 3. 정리 안 한 인접 죽은 코드 (범위 밖 — 다음 라운드 후보)

- `js/app/comparison.js` — `showComparisonView` 가 window 에 노출돼 있으나 실제 진입 경로 미확증. 씬 플레이가 죽었으니 함께 죽었을 가능성이 높다. **R5 범위(핸드아웃 명시 항목) 밖이라 손대지 않음.**
- `js/app/endScreen.js` — 위와 동일.
- `saveArchiveEmotionToPlays` — `window` 노출이 expInterview 용이었는데(그 파일은 R5-2 로 삭제) 지금은 소비자가 불명확. export 는 유지.
- `#archiveEntryContainer` DOM — 채우는 코드 없음(R5-4). 숨김 처리에만 쓰여 유지.

---

## 작업 중 발견한 위험 (지휘 세션에 보고)

**여러 수리 레인이 같은 작업 트리에서 동시에 일하고 있다.** R5-2 검증 중 `npm test` 가 3건 깨져 원인을 추적했더니, 내 변경이 아니라 **R4 레인이 커밋하지 않은 `js/shared/math.js` 수정**(pipeline.regression → ByeoriEngine → math.js)이었다. 테스트 개수도 작업 도중 534 → 542 → 554 → 564 로 계속 변했다.

- 대응: 나는 `git add -A` 를 한 번도 쓰지 않고 **매 커밋마다 내 파일 경로만 명시적으로 스테이징**했다. 남의 WIP 를 내 커밋에 섞지 않았다.
- 권고: 레인 간 파일 소유권은 지켜졌으나(충돌 0), `npm test` 를 게이트로 쓰는 레인은 **실패가 자기 것인지 남의 WIP 인지 반드시 확인**해야 한다. 최종 통합 시 전체 재검증 권장.

## 최종 상태

```
9 files changed, 74 insertions(+), 2603 deletions(-)
npm test: 564 passed (564), 22 files
```

첫 로드 체인에서 빠진 것: live.js(3,080줄, 동적 전환) + expInterview.js(820줄) + registration.js(505줄) + archiveEntry.js(253줄) + archive.js 씬 플레이(726줄).
