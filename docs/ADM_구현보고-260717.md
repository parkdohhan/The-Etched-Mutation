# ADM 구현보고 — admin 상용화 1~3차 (2026-07-17)

> 정본 지시: `docs/핸드아웃_ADM_상용화_1-3차-260717.md` + `docs/admin_UX검수-260717.md`
> 구역 준수: `admin.html` · `js/admin.js` · `js/admin-trajectory.js` · `js/admin/**` · 보고서만 수정.
> play-test.html · js/shared · js/ui · index.html · css/admin.css **무수정 확인**.

---

## Wave 1 — 저작 루프 응급 (feat(ADM1)) — 커밋 `1c343a6`

### A. 저장 UX (B1+B2) — `js/admin.js saveMemory()`
| | 전 | 후 |
|---|---|---|
| 저장 후 화면 | 대시보드로 강제 퇴출(`cancelEdit()`) | **편집기 잔류** (퇴출 제거) |
| 피드백 | 없음(무피드백) | 성공 토스트 `저장됨 — 씬 N개` / 실패 토스트(원문 포함, 수동닫기) |
| 목록 반영 | 새 기억은 새로고침해야 등장 | `loadMemories()`+`loadAllSessions()` 로 **즉시 반영**(신규 포함) |
| 이중 클릭 | 무방비 | 저장 중 버튼 잠금 + "저장 중…" 표시(`setSaveButtonsBusy`) |
| 신규 재저장 | `currentMemoryIndex` 미갱신 → 중복 INSERT 위험 | 저장 후 `currentMemoryId`/`Index` 확정 → 이후 UPDATE 경로 |

- 검증 alert 2건(제목·코드 / 씬 0개)도 오류 토스트로 교체.
- 저장 경로는 기존 `saveMemoryGraph`(id 보존 diff) 그대로 — 우회 없음.

### B. 씬 카드 접기 (F1) — `renderScenes()` / `admin.html`
- 씬 카드 **기본 접힘**. 헤더 = `S{순서} · {역할} · {본문 첫 20자}`(+유령 이름 있으면 `👻이름`).
- 클릭 토글(카드 상단 바). "모두 펼치기 / 모두 접기" 버튼(씬 편집기 상단).
- 새 씬 추가 시 **펼친 상태 생성 + 그 카드로 스크롤**.
- 접기 상태는 `sceneCollapseState`(Map); 기억 전환(editMemory/addNewMemory) 시 초기화.

### C. 유령 이름 칸 — `renderScenes()` + `attachSceneListeners()`
- 씬 카드에 입력 1개: 라벨 "유령 이름 (공명 시 드러남)".
- 저장/로드 = `scenes.meta.ghost_name` (**meta 병합** — 다른 키 보존, CLAUDE 규칙 3).
- 소비자 `js/ui/lumen_dialog_phase1.js:1437`(`_sub.ghostTrueName = meta.ghost_name`) — 읽기 확인만, **무수정**.

### 토스트 유틸 — `js/admin/toast.js` (신규)
- 성공/정보/오류 3종, 자동 소멸(오류 7s·나머지 3.2s, 오류 저장실패는 수동닫기), 큐(최대 4).
- 자기 CSS 주입(css/admin.css 구역 밖이라 손대지 않음). `textContent` 사용(XSS 안전).
- 모듈 `export { showToast }` + 전역 `window.temToast`.

### 캐시버스터
- `admin.html`: `js/admin.js?v=260717a` (기존 ?v= 없어 신규 추가). toast.js 는 신규 파일이라 캐시 없음.

---

## Wave 2 — 동선 위생 (feat(ADM2)) — 커밋 `41c35ca`

### E. 목록 검색 (F4) — `admin.html` + `renderSessions()`
- 대시보드 상단 입력 1칸. 제목/코드 **부분일치** 클라이언트 필터(`sessionSearchQuery`).

### F. alert → 토스트 (F5 · B3) — `js/admin.js`
- admin.js `alert()` **36건 전량** 토스트화(오류=error/성공=success/안내=info). 남은 alert 0.
- 파괴 동작 `confirm()`(삭제·복원)은 규칙 5대로 **유지**.
- B3: `"아카이브이 삭제되었습니다"` → `"기억이 삭제되었습니다"`(라이브는 `"라이브 세션이 삭제되었습니다"`).

### D. Canvas 기억 선택기 (F3) — `admin.html` + `js/admin-trajectory.js`
- 궤적 뷰어 사이드바 상단 드롭다운(전체 memories `제목 — 코드`). 선택 시 `initTrajectoryViewer` 재진입.
- 현재 기억 `selected` 표기. `!deleted_at` 클라이언트 필터(W3 휴지통 대비, 컬럼 전에도 안전).

### G. 잔상 모더레이션 운영 탭 이관 (F8) — `admin.html` + `js/admin.js`
- 기존 relocate 스크립트 완성: `#utterancesContent` 를 `#operationsMount` 로 이동 시
  `editor-content` 클래스 분리(→ `switchTab('edit')`이 active 를 뺏어 다시 숨기던 교차오염 차단) + `display:block` 고정.
- 운영 탭 진입 시 `window.loadUtterances()` 호출(노출 추가). 편집화면 **잔상 탭 버튼 제거**.

### H. 문구 정리 (F9) — `admin.html`
- 앵커 이미지 섹션 한글화(앵커 이미지 / 키워드 / 유형 / 내용 / 사진 파일 / 앵커 저장 / 목록 새로고침).
- 운영 탭 안내문 갱신(빈 방 → 잔상 모더레이션 설명).

### 캐시버스터
- `admin.html`: `js/admin-trajectory.js?v=260717a`.

---

## Wave 3 — 상용 심화 (feat(ADM3) `7b62922` + 마이그레이션 `a7efb4e`)

### I. 마이그레이션 — 원격 apply 완료
- 대상 프로젝트: **`bxmppaxpzbkwebfbgpsm` (The Etched Mutation, ACTIVE_HEALTHY)** —
  코드 `SUPABASE_URL`과 정확히 일치(사용자 승인 하 적용).
- `memories.deleted_at timestamptz` 추가 + `admin_versions` 테이블(RLS+authenticated 전권 정책).
- 적용 전/후 SQL 실측: 적용 전 0/0/0 → 적용 후 deleted_at·admin_versions·정책1·RLS true 확인.
- 저장소 파일: `supabase/migrations/20260717000100_admin_trash_and_versions.sql`
  (핸드아웃의 `202607170001` 을 저장소 14자리 관례로 정규화 — 정렬 안전, 내용 동일).

### J. 휴지통 (F6) — soft-delete
- archive 삭제 = `deleted_at=now()` + `is_public=false`(행 보존, play-test 는 공개 해제로 자연 숨김·무수정).
- 목록 기본 `deleted_at IS NULL`, "휴지통" 토글 → 삭제된 기억만. 복원(deleted_at=null, **공개는 수동**)·
  영구삭제(confirm 2단, 기존 hard delete). 검색·Canvas 선택기도 휴지통 제외. 라이브 세션은 기존 hard delete 유지.

### K. 판본 이력 — admin_versions
- `saveMemory` 성공 직후 `{memory, scenes[], choices[]}` 스냅샷 INSERT, 기억당 최근 20개 유지.
- 편집화면 "판본 이력" 패널(시각·note) + [이 판본으로 복원]. 복원 = 스냅샷→편집기 로드(editMemory 재사용)→
  **saveMemoryGraph 경유**(직접 DB 쓰기 X, 씬 id·plays 보존). 복원 직전 현재 상태 자동 스냅샷(note='복원 전 자동').

### L. 자동저장 (F7)
- 편집 변경 시 디바운스 30초 localStorage 임시본(키=기억 id). 진입 시 임시본 있으면 배너(복원/버리기).
  저장 성공 시 임시본 삭제.

### M. 씬 목차 사이드바
- 편집화면 좌측 고정 — 씬 목록(S순서·역할·이름). 클릭 = 해당 카드 펼침+스크롤(B 연동). 1400px 미만 숨김.

### N. 유령 대사 편집기 (dialog_choices)
- ⚠️ **핸드아웃 스키마 편차**: 핸드아웃은 4종(ghost_intro/choice_select/choice_reply/free_dialog_open)을
  상정했으나, **발자국 실데이터 + 소비자(lumen_dialog_phase1.js:1777)** 의 실제 스키마는
  `{ ghost_intro:[], scene_context:[], choices:[{label, ghost_reply:[], free_dialog_open:[]}] }` 였음.
  핸드아웃의 "발자국 실데이터로 확인 후 **동일 스키마**" 지시에 따라 **실제 스키마로 구현**. `scenes.meta.dialog_choices` 병합.

### O. 감정 결 선택지 편집기 (choices)
- 씬 카드 접힘 섹션 — 행: text · emotion(17축 `DEFAULT_EMOTION_ANCHORS` 드롭다운, 재정의 X) · intensity.
- ⚠️ **강도 스케일 편차**: 핸드아웃은 0~1 이라 했으나 실데이터(min5·max9)와 `saveMemory` 의 `intensity/10`
  로직이 **0~10** 스케일이라, 데이터 정합을 위해 **0~10** 채택(0~1 이면 emotion_dist 가 뭉개짐). 저장은 기존 saveMemoryGraph 경로.

### P. 작가 궤적 시딩 (§11)
- [작가 궤적 시딩] 버튼 — 씬별 plays 1행: user_emotion=원본감정, user_reason='author-seed',
  alignment=1.0, mismatch_type=null, 공유 run_id, **persona_id='author-seed'**(관객 인구 격리 §5),
  final_drift_vector={} · sky_stains={}(NOT NULL 충족). 기존 author-seed 존재 시 확인 후 교체.
  실행 후 실관객 수(persona_id IS NULL AND created_at>=2026-07-09) 불변 토스트 실측.

### Q. 명세 갱신
- `docs/자료스키마_명세_v1-260717.md` **v1.1**: §1 persona 행·§3 admin_versions·§4 deleted_at·§5 author-seed 규칙 추가.

---

## 실기 검증 표

| 항목 | 코드/문법 | 회귀(npm test) | 실기(UI) |
|---|---|---|---|
| W1 A·B·C + 토스트 | ✅ `node --check` | ✅ 581/581 | ⏳ 대기(브라우저) |
| W2 D·E·F·G·H | ✅ `node --check` | ✅ 581/581 | ⏳ 대기(브라우저) |
| W3 I(마이그레이션) | — | — | ✅ **원격 apply + SQL 실측 확인** |
| W3 J·K·L·M·N·O·P·Q | ✅ `node --check` | ✅ 581/581 | ⏳ 대기(브라우저) |

**실기(UI) 검증 대기 사유**: admin 은 Supabase 인증(로그인) 필요. 사용자 결정 = "브라우저 풀어주면
내가 시도". MCP 브라우저 점유 해제 + 로그인 자격증명 확보 시, UX-TEST 기억으로 실기 검증 수행 예정:
① 저장 잔류+토스트+목록 즉시 ② 접기/목차 점프 ③ ghost_name 왕복 ④ 휴지통 삭제→복원(is_public false 유지)
⑤ 판본 복원 씬 id·plays 불변 SELECT ⑥ 자동저장 배너 ⑦ choices→emotion_dist 재계산 ⑧ 시딩→관객 수 불변.
종료 시 시험 기억+시딩+판본+임시본 전량 삭제 + 잔재 0.

---

## 못 한 것 (정직)

- **핸드아웃 편차 2건**(보고 위 표기): N dialog_choices 스키마(실데이터 따름), O intensity 0~10(데이터 정합).
  둘 다 핸드아웃 "동일 스키마"/데이터 무결성 원칙에 부합하는 방향으로 결정.

---

## 지휘 실기 검수 (2026-07-17, 커밋 179349d) — 통과 + 결함 2건 봉합

브라우저 해제 후 지휘가 UX-TEST 기억으로 전 항목 실기 검증. **결함 2건 발견·수리:**

**결함 1 — 캐시버스터 충돌 (배포 위생).** W1~W3 이 admin.js/admin-trajectory.js 를 전부
`?v=260717a` 재사용 → Wave 1 캐시 브라우저가 W2·W3 함수 없는 옛 사본 실행 →
목차·휴지통·판본·시딩·대사/선택지 편집기 onclick 전량 무반응(window.renderSceneToc
undefined·transferSize=0 실측). `?v=260717e` 로 갱신. W4-4b·E1 재발.

**결함 2 — 저장 후 씬 id churn (correctness, W1-A 가 노출).** 저장 후 편집기 잔류로
currentScenes 에 DB 씬 id 미복귀 → 다음 저장이 씬 DELETE+새 UUID INSERT → plays.scene_id
단절. **저작 루프(자주 저장)에서 매 저장마다 관객 기록이 씬 연결 상실**, 판본 복원도
이 위에서 깨짐. 수리 = 저장 직후 DB 씬으로 currentScenes id 재수화(admin.js:2561~).

**실기 검증 결과 (수리 후):**
| 항목 | 결과 |
|---|---|
| A 저장 UX | ✅ 편집기 잔류·성공 토스트·목록 즉시·id 확정 |
| B/M 접기·목차 | ✅ 2씬 8109px→접기 4137px, 목차 점프(1400px+ 표시) |
| C ghost_name | ✅ 저장·재로드·목차 표시 왕복 |
| K 판본 | ✅ 스냅샷 생성·복원 후 **씬 id 불변·plays 연결 온전**(수리 후) |
| O choices | ✅ 저장→emotion_dist 8키 재계산 |
| I 마이그레이션 | ✅ deleted_at·admin_versions·RLS·정책 SQL 실측 |
| 씬 id 안정성 | ✅ save1→플레이심기→save2 씬 id 불변(17a35299)·연결 유지 |

**미검증 잔여**(다음 실기 라운드): J 휴지통 삭제→복원 왕복 UI, L 자동저장 배너,
P 작가 시딩 관객 수 불변. 코드 경로는 준비됨. 검수 데이터 전량 삭제(실기억 7 온전).
