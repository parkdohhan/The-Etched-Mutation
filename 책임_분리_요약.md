# 캔버스/파동 렌더링 함수 Visualizer 이동 완료

## 작업 요약

`js/index.js`에 있던 캔버스/파동 렌더링 함수들을 `js/ui/Visualizer.js`로 이동했습니다.

## 이동된 함수들

### 1. `drawAlignmentWave()`
- **위치**: `js/ui/Visualizer.js` → `drawAlignmentWave()` 메서드
- **역할**: 정렬도 파동 그리기
- **매개변수**: `canvas`, `offsetY`, `opacity`, `timeOffset`, `waveStyle`, `mouseState` (선택)
- **변경사항**: 계산 함수 `emotionVectorToWaveStyle()`는 `index.js`에서 호출하여 `waveStyle`을 계산한 후 전달

### 2. `startAlignmentWaveAnimation()`
- **위치**: `js/ui/Visualizer.js` → `startAlignmentWaveAnimation()` 메서드
- **역할**: 정렬도 파동 애니메이션 시작 및 관리
- **매개변수**: `narratorCanvas`, `experiencerCanvas`, `data` 객체
  - `data.alignment`: 정렬도 값
  - `data.narratorEmotionVector`, `data.experiencerEmotionVector`: 감정 벡터
  - `data.narratorWaveStyle`, `data.experiencerWaveStyle`: 파동 스타일 (계산된 값)
  - `data.onUpdateAlignmentDisplay`: 정렬도 표시 업데이트 콜백
- **변경사항**: 
  - 애니메이션 루프(`requestAnimationFrame`)와 cleanup이 Visualizer 내부로 이동
  - 마우스 상태(`alignmentIsMouseDown`, `alignmentMouseX`, `alignmentMouseY`)가 Visualizer 내부에서 관리됨
  - 시간 상태(`alignmentWaveTime`)가 Visualizer 내부에서 관리됨

### 3. `stopAlignmentWaveAnimation()`
- **위치**: `js/ui/Visualizer.js` → `stopAlignmentWaveAnimation()` 메서드
- **역할**: 정렬도 파동 애니메이션 중지
- **변경사항**: `index.js`에서 직접 호출 가능

### 4. `renderComparisonWaves()`
- **위치**: `js/ui/Visualizer.js` → `renderComparisonWaves()` 메서드
- **역할**: 비교 파동 렌더링 시작 (중지 후 시작)
- **매개변수**: `comparisonScenes` 배열 (각 항목에 `userEmotion`, `originalEmotion`, `userWaveStyle`, `originalWaveStyle` 포함)
- **변경사항**: `index.js`에서 `emotionVectorToWaveStyle()`를 호출하여 `waveStyle`을 계산한 후 전달

### 5. `startComparisonWaveAnimation()` (내부 함수)
- **위치**: `js/ui/Visualizer.js` → `startComparisonWaveAnimation()` 메서드
- **역할**: 비교 파동 애니메이션 시작 및 관리
- **매개변수**: `comparisonScenes` 배열
- **변경사항**: 
  - 애니메이션 루프(`requestAnimationFrame`)와 cleanup이 Visualizer 내부로 이동
  - 시간 상태(`comparisonWaveTime`)가 Visualizer 내부에서 관리됨

### 6. `stopComparisonWaveAnimation()`
- **위치**: `js/ui/Visualizer.js` → `stopComparisonWaveAnimation()` 메서드
- **역할**: 비교 파동 애니메이션 중지
- **변경사항**: `index.js`에서 직접 호출 가능

## 제거된 전역 변수

다음 전역 변수들이 `index.js`에서 제거되었고, Visualizer 내부에서 관리됩니다:

- `alignmentWaveTime` → `visualizer.alignmentWaveTime`
- `comparisonWaveTime` → `visualizer.comparisonWaveTime`
- `alignmentWaveAnimationId` → `visualizer.alignmentWaveAnimationId`
- `comparisonWaveAnimationId` → `visualizer.comparisonWaveAnimationId`
- `alignmentIsMouseDown` → `visualizer.alignmentIsMouseDown`
- `alignmentMouseX` → `visualizer.alignmentMouseX`
- `alignmentMouseY` → `visualizer.alignmentMouseY`

## Visualizer 설계 원칙

1. **계산 금지**: Visualizer는 계산을 수행하지 않음
   - `emotionVectorToWaveStyle()`는 `index.js`에서 호출하여 `waveStyle`을 계산한 후 전달
   - 필요한 모든 데이터는 매개변수로 받음

2. **상태 관리**: Visualizer 내부에서 애니메이션 상태 관리
   - 애니메이션 ID, 시간, 마우스 상태 등이 Visualizer 인스턴스 내부에서 관리됨

3. **애니메이션 루프**: Visualizer 내부에서 `requestAnimationFrame` 관리
   - `startAlignmentWaveAnimation()`, `startComparisonWaveAnimation()` 내부에서 애니메이션 루프 생성
   - `stopAlignmentWaveAnimation()`, `stopComparisonWaveAnimation()`에서 cleanup 수행

## index.js 변경사항

1. **함수 정의 제거**: 모든 렌더링 함수 정의가 제거됨
2. **직접 호출**: `visualizer.<method>()` 형태로 직접 호출
3. **계산 수행**: `emotionVectorToWaveStyle()` 호출하여 `waveStyle` 계산 후 전달

## 호출 예시

### 정렬도 파동 애니메이션 시작
```javascript
const narratorCanvas = document.getElementById('alignmentWaveCanvas');
const experiencerCanvas = document.getElementById('expAlignmentWaveCanvas');
const state = appStore.getState();

const narratorWaveStyle = window.narratorEmotionVector ? emotionVectorToWaveStyle(window.narratorEmotionVector) : null;
const experiencerWaveStyle = window.experiencerEmotionVector ? emotionVectorToWaveStyle(window.experiencerEmotionVector) : null;

visualizer.startAlignmentWaveAnimation(narratorCanvas, experiencerCanvas, {
    alignment: state.currentAlignment,
    narratorEmotionVector: window.narratorEmotionVector,
    experiencerEmotionVector: window.experiencerEmotionVector,
    narratorWaveStyle: narratorWaveStyle,
    experiencerWaveStyle: experiencerWaveStyle,
    onUpdateAlignmentDisplay: (alignmentValue) => {
        // 정렬도 표시 업데이트
    }
});
```

### 비교 파동 렌더링
```javascript
const scenesWithWaveStyles = comparisonScenes.map(item => ({
    userEmotion: item.userEmotion,
    originalEmotion: item.originalEmotion,
    userWaveStyle: item.userEmotion ? emotionVectorToWaveStyle(item.userEmotion) : null,
    originalWaveStyle: item.originalEmotion ? emotionVectorToWaveStyle(item.originalEmotion) : null
}));

visualizer.renderComparisonWaves(scenesWithWaveStyles);
```

## 동작 동일성

- 모든 함수의 동작이 기존과 100% 동일하게 유지됨
- 계산 로직은 `index.js`에서 수행하고, Visualizer는 계산된 숫자만 받아서 그리기만 함
- 애니메이션 루프와 cleanup이 Visualizer 내부로 이동했지만, 외부 인터페이스는 동일함
