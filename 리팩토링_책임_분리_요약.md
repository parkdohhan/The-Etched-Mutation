# 리팩토링 책임 분리 요약 문서

## 개요

이 문서는 `js/index.js`의 리팩토링 과정에서 이루어진 책임 분리와 모듈화 작업을 정리한 것입니다.

---

## 1. ByeoriEngine (SSOT) - 계산 로직 중앙화

### 목적
정렬도, 버킷, 미스매치 계산 로직을 단일 소스로 통합하여 일관성과 유지보수성 향상

### 파일
- `js/core/ByeoriEngine.js`

### 책임
- **정렬도 계산**: 복합 정렬도 계산 (감정 40% + 이유 40% + VOID 20%)
- **버킷 판정**: 히스테리시스 및 FIXATED 조건 포함
- **미스매치 판정**: 사용자/원본 벡터 간 불일치 타입 판정
- **VAD 투영**: 감정 벡터를 VAD 좌표로 변환

### API
```javascript
const engineResult = byeoriEngine.calculateStep(
  { userVector, originalVector, anchorEmotions },
  { previousBucket, emotionHistory }
);
// 반환: { alignment_score, alignment_bucket, mismatch_type, ... }
```

### 제거된 코드
- `index.js`에서 `calculateComplexAlignment()`, `getMismatchType()` 직접 호출 제거
- 모든 계산 로직이 `ByeoriEngine.calculateStep()`로 통일

---

## 2. Simple Store - 전역 변수 제거

### 목적
전역 변수를 store로 중앙화하여 상태 관리 일관성 확보

### 파일
- `js/core/store.js`

### 책임
- **상태 저장**: 애플리케이션 전역 상태 관리
- **상태 업데이트**: `setState()`를 통한 shallow merge
- **상태 구독**: `subscribe()`를 통한 반응형 업데이트

### API
```javascript
const appStore = createStore(initialState);
appStore.getState();        // 상태 읽기
appStore.setState({ ... }); // 상태 업데이트
appStore.subscribe(fn);     // 상태 변경 구독
```

### 이동된 변수들
- `currentMode`, `currentScene`, `currentMemory`
- `currentAlignment`, `currentBucket`
- `emotionHistory`, `userChoices`, `userReasons`
- `currentSessionId`, `currentUser`
- 기타 전역 상태 변수들

---

## 3. NetworkService - Supabase 호출 격리

### 목적
모든 Supabase 네트워크 호출을 단일 서비스로 중앙화

### 파일
- `js/services/NetworkService.js`

### 책임
- **메모리 관리**: `fetchMemories()`, `getMemoryById()`, `saveMemory()`
- **세션 관리**: `createSession()`, `joinSession()`, `endSession()`
- **장면 관리**: `saveScene()`, `getScenesByMemoryId()`
- **플레이 저장**: `savePlay()`, `getPlayBySceneAndMemory()`
- **기타**: 쪽지, 오염도, AI 함수 호출 등

### API
```javascript
const result = await networkService.savePlay(playData);
// 반환: { ok: boolean, data: Object|null, error: Error|null }
```

### 제거된 코드
- `index.js`에서 `supabase.from(...)` 직접 호출 제거
- 모든 네트워크 로직이 `NetworkService`로 이동

---

## 4. RealtimeService - 실시간 구독 격리

### 목적
Supabase realtime 구독 로직을 단일 서비스로 중앙화

### 파일
- `js/services/RealtimeService.js`

### 책임
- **채널 관리**: Supabase realtime 채널 생성/해제
- **구독 관리**: `subscribeToLiveScenes()`, `subscribeToScenes()` 등
- **인터벌 관리**: `registerInterval()`, `clearInterval()`
- **정리**: `cleanup()`을 통한 모든 구독/인터벌 정리

### API
```javascript
realtimeService.subscribeToLiveScenes(sessionId, handlers);
realtimeService.cleanup(); // 모든 구독 정리
```

### 제거된 코드
- `index.js`에서 `supabaseClient.channel()` 직접 호출 제거
- 모든 realtime 로직이 `RealtimeService`로 이동

---

## 5. UIManager - DOM 조작 격리

### 목적
DOM 조작 및 화면 전환 로직을 단일 매니저로 중앙화

### 파일
- `js/ui/UIManager.js`

### 책임
- **화면 전환**: `showMypage()`, `closeMypage()`
- **정렬도 표시**: `updateAlignmentDisplay()`, `updateExperiencerAlignmentDisplay()`
- **메모리 카드 렌더링**: `renderMemoryCards()`
- **장면 표시**: `displaySceneForExperiencer()`
- **선택지 렌더링**: `renderLiveChoices()`

### API
```javascript
uiManager.showMypage(userData);
uiManager.renderMemoryCards(memories, category, sort, callback);
uiManager.displaySceneForExperiencer(scene, callbacks);
```

### 제거된 코드
- `index.js`에서 DOM 조작 로직 제거
- 계산 로직은 매개변수로 전달받도록 변경

---

## 6. Visualizer - 캔버스 렌더링 격리

### 목적
캔버스 그리기 로직을 단일 비주얼라이저로 중앙화

### 파일
- `js/ui/Visualizer.js`

### 책임
- **아카이브 파동**: `renderArchiveEmotionWave()`
- **정렬도 파동**: `drawAlignmentWave()`, `startAlignmentWaveAnimation()`
- **비교 파동**: `renderComparisonWaves()`, `startComparisonWaveAnimation()`
- **애니메이션 관리**: `requestAnimationFrame` 루프 및 cleanup

### API
```javascript
visualizer.renderArchiveEmotionWave(canvas, waveData, time);
visualizer.startAlignmentWaveAnimation(canvas1, canvas2, options);
visualizer.renderComparisonWaves(scenesWithWaveStyles);
```

### 제거된 코드
- `index.js`에서 캔버스 그리기 로직 제거
- 계산 로직은 매개변수로 전달받도록 변경
- 애니메이션 관련 전역 변수 제거 (`alignmentWaveTime`, `comparisonWaveTime` 등)

---

## 7. submitEmotion() 리팩토링 - 거대 함수 분해

### 목적
한 줄로 압축된 거대 함수를 책임별로 분해하여 가독성 및 유지보수성 향상

### 파일
- `js/index.js` (같은 파일 내 하위 함수로 분해)

### 분해된 함수들

#### 7.1 `collectEmotionInput()`
- **책임**: DOM/store에서 입력 수집 및 검증
- **반환**: `{ reason, scene, currentData, anchorEmotions }` 또는 `null`

#### 7.2 `analyzeEmotionForArchive()`
- **책임**: 감정 분석 수행 (archive 모드일 때만)
- **반환**: `{ userEmotionVector, reasonVector }` 또는 `null`

#### 7.3 `runEngineStep()`
- **책임**: `ByeoriEngine.calculateStep()` 호출만 수행
- **반환**: 엔진 계산 결과

#### 7.4 `applyEngineResult()`
- **책임**: store 업데이트 (정렬도, 버킷, 감정 히스토리)
- **반환**: `{ sceneAlignment, newBucket, stateAfterUpdate }`

#### 7.5 `updateUIAfterSubmit()`
- **책임**: UI 업데이트 (정렬도 표시, 파동 렌더링, 알림, 버킷 피드백)

#### 7.6 `persistAfterSubmit()`
- **책임**: NetworkService를 통한 저장

#### 7.7 `submitEmotion()`
- **책임**: 오케스트레이터 (위 함수들을 순서대로 호출)

### 변경 사항
- **이전**: 한 줄로 압축된 함수 (5556자)
- **이후**: 여러 줄로 분해된 함수들 (약 200줄)
- **기능**: 100% 동일 유지

---

## 8. sendExpChatMessage() / sendChatMessage() 리팩토링

### 목적
채팅 메시지 처리 로직을 단계별 함수로 분해하고 AI 호출을 통일

### 파일
- `js/index.js` (같은 파일 내 하위 함수로 분해)

### 분해된 함수들

#### 8.1 `collectChatMessage(inputId)`
- **책임**: 메시지 입력 수집 및 검증
- **반환**: 사용자 메시지 또는 `null`

#### 8.2 `reflectExpChatMessageUI(userMessage)` / `reflectChatMessageUI(userMessage)`
- **책임**: UI에 메시지 반영

#### 8.3 `callAI(prompt, meta)` ⭐ **중요**
- **책임**: AI 호출 통일 함수 (나중에 AIService로 이동 예정)
- **타입**: `'emotion_analysis'` | `'scene_generation'`
- **메타데이터**: `{ type, reasonText?, anchorEmotions?, conversationHistory?, systemPrompt? }`
- **반환**: AI 응답 결과

#### 8.4 `parseEmotionAnalysisResult(emotionResult, userMessage)`
- **책임**: 감정 분석 결과 파싱 및 검증

#### 8.5 `parseSceneGenerationResult(aiResponse)`
- **책임**: 장면 생성 결과 파싱

#### 8.6 `persistExpEmotionResult(emotionResult, userMessage)`
- **책임**: 체험자 감정 결과 저장 및 로그

#### 8.7 `persistSceneGenerationResult(generatedText)`
- **책임**: 장면 생성 결과 저장 및 로그

#### 8.8 `sendExpChatMessage()` / `sendChatMessage()`
- **책임**: 오케스트레이터 (위 함수들을 순서대로 호출)

### TODO: window 전역 참조 → store 이동 후보
- `window.expFinalObject` → `appStore`로 이동
- `window.currentStoryData` → `appStore`로 이동
- `window.pendingSceneText` → `appStore`로 이동
- `window.currentPhase` → `appStore`로 이동
- `window.expPendingEmotion` → `appStore`로 이동

### 변경 사항
- **이전**: 한 줄로 압축된 함수들 (2163자, 1490자)
- **이후**: 여러 줄로 분해된 함수들
- **AI 호출 통일**: `analyzeEmotionWithVector()`와 `callClaudeAPI()`를 `callAI()`로 통일

---

## 9. 현재 파일별 책임 분리 현황

### `js/index.js`
- **책임**: 이벤트 바인딩 및 오케스트레이터 역할
- **호출**: `store`, `engine`, `service`, `uiManager`, `visualizer`
- **금지**: 직접 계산, DOM 조작, 네트워크 호출, 캔버스 그리기

### `js/core/ByeoriEngine.js`
- **책임**: 정렬도/버킷/미스매치 계산 (순수 함수)
- **금지**: DOM 접근, Supabase 호출, 전역 변수 접근

### `js/core/store.js`
- **책임**: 애플리케이션 상태 관리
- **제공**: `getState()`, `setState()`, `subscribe()`

### `js/services/NetworkService.js`
- **책임**: 모든 Supabase 네트워크 호출
- **금지**: 계산 로직, DOM 접근

### `js/services/RealtimeService.js`
- **책임**: Supabase realtime 구독 관리
- **금지**: 계산 로직, DOM 접근

### `js/ui/UIManager.js`
- **책임**: DOM 조작 및 화면 전환
- **금지**: 계산 로직, 네트워크 호출

### `js/ui/Visualizer.js`
- **책임**: 캔버스 렌더링
- **금지**: 계산 로직, DOM 접근 (캔버스 제외)

---

## 10. 향후 리팩토링 계획

### AIService 생성 (우선순위: 높음)
- `callAI()` 함수를 `js/services/AIService.js`로 이동
- 모든 AI 호출을 단일 서비스로 통합
- 에러 처리 및 재시도 로직 중앙화

### ChatService 생성 (우선순위: 중간)
- `sendExpChatMessage()`, `sendChatMessage()` 로직을 `js/services/ChatService.js`로 이동
- 채팅 메시지 처리 로직 중앙화

### window 전역 변수 제거 (우선순위: 중간)
- `window.expFinalObject`, `window.currentStoryData` 등을 `appStore`로 이동
- 전역 상태 관리 완전 통합

### saveMemoryToDB() 분해 (우선순위: 낮음)
- 257줄의 거대 함수를 단계별 함수로 분해
- 검증 로직과 저장 로직 분리

---

## 11. 리팩토링 효과

### 가독성
- 한 줄로 압축된 함수 → 여러 줄로 분해
- 함수명으로 책임 명확화

### 유지보수성
- 단일 책임 원칙 준수
- 테스트 가능한 작은 함수들

### 확장성
- AIService, ChatService 등으로 추가 분리 가능
- 모듈 간 의존성 명확화

### 디버깅
- 각 단계별 함수로 분리되어 디버깅 용이
- 에러 발생 지점 명확화

---

## 12. 참고사항

- 모든 리팩토링은 **기능 변경 없이** 수행됨
- 기존 동작과 100% 동일하게 유지
- 점진적 리팩토링으로 안정성 확보
